def linearSearch(list,n,key):
    for i in range(0,n):
        if list[i] == key:
              return i
    return -1
   
list = [1,2,3]
key = 2
   
n = len(list)
pos = linearSearch(list,n,key)
if(pos == -1):
   print("Element not found")
else:
   print("Element found at position",pos)
